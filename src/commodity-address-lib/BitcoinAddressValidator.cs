using System;
using validation_lib;
using validation_lib.Models;

namespace commodity_address_lib
{
    public class BitcoinAddressValidator : IValidator<string>
    {
        public ValidationResult Validate(string address)
        {
            throw new NotImplementedException();
        }
    }
}
